version: "3.9"

services:
  web:
    build: .
    command: gunicorn --bind 0.0.0.0:8000 --workers 3 main.wsgi:application
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env



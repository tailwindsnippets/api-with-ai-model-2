version: "3.9"

services:
  web:
    build: .
    command: gunicorn --bind 0.0.0.0:8001 --workers 3 main.wsgi:application
    volumes:
      - .:/app
    ports:
      - "8001:8001"
    env_file:
      - .env


